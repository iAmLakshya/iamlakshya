# Settings for Cloud Run
runConfig:
  minInstances: 0
  maxInstances: 100
  concurrency: 100
  cpu: 1
  memoryMiB: 512

# Environment variables and secrets
env:
  - name: ENVIRONMENT
    value: "production"